export const MAX_LEAD_FILE_SIZE_MB = 4;
export const MAX_LEAD_FILE_SIZE_BYTES =
  MAX_LEAD_FILE_SIZE_MB * 1024 * 1024;

export const MAX_LEAD_FILE_SIZE_ERROR = `Файл слишком большой. Максимальный размер — ${MAX_LEAD_FILE_SIZE_MB} МБ.`;
